<%= form_for(@deporte) do |f| %>
  <% if @deporte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deporte.errors.count, "error") %> prohibited this deporte from being saved:</h2>

      <ul>
      <% @deporte.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_area :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
