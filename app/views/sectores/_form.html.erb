<%= form_for(@sectore) do |f| %>
  <% if @sectore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sectore.errors.count, "error") %> prohibited this sectore from being saved:</h2>

      <ul>
      <% @sectore.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label "Estadio" %><br />
    <%= f.select :estadio_id, Estadio.order('nombre ASC').all.map {|r| [r.nombre,r.id] } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
